# gohugoio/hugo/hugo-extended

This branch holds the generated registry.json of the aqua package "gohugoio/hugo/hugo-extended", one per
version under `versions/`. It is created and updated by ar2 and shares no history
with the default branch.
