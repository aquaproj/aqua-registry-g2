# helix-editor/helix

This branch holds the generated registry.json of the aqua package "helix-editor/helix", one per
version under `versions/`. It is created and updated by ar2 and shares no history
with the default branch.
