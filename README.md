# kubernetes/kubernetes/kube-log-runner

This branch holds the generated registry.json of the aqua package "kubernetes/kubernetes/kube-log-runner", one per
version under `versions/`. It is created and updated by ar2 and shares no history
with the default branch.
