# godotengine/godot

This branch holds the generated registry.json of the aqua package "godotengine/godot", one per
version under `versions/`. It is created and updated by ar2 and shares no history
with the default branch.
