# protocolbuffers/protobuf/protoc

This branch holds the generated registry.json of the aqua package "protocolbuffers/protobuf/protoc", one per
version under `versions/`. It is created and updated by ar2 and shares no history
with the default branch.
